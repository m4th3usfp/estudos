/*function removerLinha(linha) {
    let tabela = document.querySelector('tbody')
    let remover = tabela.querySelector(`tr:nth-child(${linha})`);
    remover.remove(remover);

};
*/

    /*function removerLinha(linha) {
        let tbody = document.querySelector('.table-body2');
        tbody.removeChild(linha)
    } 
    */
    function removerLinha(linha) {
       linha.remove();
    };
